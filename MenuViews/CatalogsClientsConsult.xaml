<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="TextileSystem.MenuViews.CatalogsClientsConsult"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:TextileSystem.Controls"
    xmlns:editors="clr-namespace:Syncfusion.Maui.Inputs;assembly=Syncfusion.Maui.Inputs"
    xmlns:input="clr-namespace:Syncfusion.Maui.Core;assembly=Syncfusion.Maui.Core"
    xmlns:sfPopup="clr-namespace:Syncfusion.Maui.Popup;assembly=Syncfusion.Maui.Popup"
    xmlns:vm="clr-namespace:TextileSystem.MenuViewModels"
    x:DataType="vm:CatalogsClientsConsultViewModel">

    <Grid
        Padding="10"
        ColumnDefinitions="1200,*"
        RowDefinitions="46,*"
        RowSpacing="10">

        <editors:SfComboBox />
        <controls:CustomButton
            x:Name="PopupAnchor"
            Grid.Column="1"
            Command="{x:Binding CreateNewClientCommand}"
            CornerRadius="8"
            HorizontalOptions="End"
            Text="Nuevo Cliente"
            VerticalOptions="Center" />

        <Border Grid.Row="1" Grid.ColumnSpan="2">
            <CollectionView>
                <CollectionView.Header>
                    <Grid ColumnDefinitions="300,300,300,300,60">
                        <Label FontAttributes="Bold" Text="Nombre" />
                        <Label
                            Grid.Column="1"
                            FontAttributes="Bold"
                            Text="Direcccion" />
                        <Label
                            Grid.Column="2"
                            FontAttributes="Bold"
                            Text="telefono" />
                        <Label
                            Grid.Column="3"
                            FontAttributes="Bold"
                            Text="Observaciones" />
                        <Label
                            Grid.Column="4"
                            FontAttributes="Bold"
                            Text="Estado" />
                    </Grid>
                </CollectionView.Header>
            </CollectionView>
        </Border>

        <sfPopup:SfPopup
            AnimationMode="Zoom"
            AutoSizeMode="Height"
            IsOpen="{x:Binding IsCreateClientDialogOpen}"
            RelativePosition="AlignBottomRight"
            RelativeView="{x:Reference PopupAnchor}"
            ShowHeader="False"
            StaysOpen="True"
            WidthRequest="400">
            <sfPopup:SfPopup.PopupStyle>
                <sfPopup:PopupStyle CornerRadius="8" PopupBackground="{x:AppThemeBinding Dark={x:StaticResource Black}, Light={x:StaticResource White}}" />
            </sfPopup:SfPopup.PopupStyle>
            <sfPopup:SfPopup.ContentTemplate>
                <DataTemplate>
                    <Grid Padding="10" RowDefinitions="80,200,80,200,*">
                        <input:SfTextInputLayout Hint="Nombre">
                            <Entry />
                        </input:SfTextInputLayout>
                        <input:SfTextInputLayout Grid.Row="1" Hint="Dirrecion">
                            <Editor />
                        </input:SfTextInputLayout>
                        <input:SfTextInputLayout Grid.Row="2" Hint="Telefonos">
                            <Entry />
                        </input:SfTextInputLayout>
                        <input:SfTextInputLayout Grid.Row="3" Hint="Observaciones">
                            <Entry IsPassword="True" />
                        </input:SfTextInputLayout>
                        <HorizontalStackLayout
                            Grid.Row="4"
                            HorizontalOptions="End"
                            Spacing="10">
                            <controls:CustomButton Command="{x:Binding ResetTexBoxCommand}" Text="borrar" />
                            <controls:CustomButton Command="{x:Binding SaveToDatabaseCommand}" Text="Guardar" />
                        </HorizontalStackLayout>
                    </Grid>
                </DataTemplate>
            </sfPopup:SfPopup.ContentTemplate>
        </sfPopup:SfPopup>
    </Grid>
</ContentPage>