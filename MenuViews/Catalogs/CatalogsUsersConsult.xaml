<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="TextileSystem.MenuViews.CatalogsUsersConsult"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:app="http://schemas.textilesystem.com/maui"
    xmlns:editors="clr-namespace:Syncfusion.Maui.Inputs;assembly=Syncfusion.Maui.Inputs"
    xmlns:inputLayout="clr-namespace:Syncfusion.Maui.Core;assembly=Syncfusion.Maui.Core"
    xmlns:loc="clr-namespace:LocalizationResourceManager.Maui;assembly=LocalizationResourceManager.Maui"
    xmlns:pop="clr-namespace:Syncfusion.Maui.Popup;assembly=Syncfusion.Maui.Popup"
    x:DataType="app:CatalogsUsersConsultViewModel">

    <Grid
        Padding="10"
        ColumnDefinitions="400,*"
        RowDefinitions="46,*"
        RowSpacing="10">

        <editors:SfComboBox />

        <app:CustomButton
            x:Name="PopupAnchor"
            Grid.Column="1"
            Command="{x:Binding CreateUserColorCommand}"
            CornerRadius="8"
            HorizontalOptions="End"
            Text="Nuevo usuario"
            VerticalOptions="Center" />

        <Border Grid.Row="1" Grid.ColumnSpan="2">
            <CollectionView>
                <CollectionView.Header>

                    <Grid ColumnDefinitions="300,300,300,60">
                        <Label FontAttributes="Bold" Text="{loc:Translate Ui_Name}" />
                        <Label
                            Grid.Column="1"
                            FontAttributes="Bold"
                            Text="{loc:Translate Ui_Username}" />
                        <Label
                            Grid.Column="2"
                            FontAttributes="Bold"
                            Text="{loc:Translate Ui_Password}" />
                        <Label
                            Grid.Column="3"
                            FontAttributes="Bold"
                            Text="{loc:Translate Ui_Status}" />
                    </Grid>
                </CollectionView.Header>
            </CollectionView>
        </Border>

        <pop:SfPopup
            AnimationMode="Zoom"
            AutoSizeMode="Height"
            IsOpen="{x:Binding IsCreateUsersDialogOpen}"
            RelativePosition="AlignBottomRight"
            RelativeView="{x:Reference PopupAnchor}"
            ShowHeader="False"
            StaysOpen="True"
            WidthRequest="400">

            <pop:SfPopup.PopupStyle>
                <pop:PopupStyle CornerRadius="8" PopupBackground="{x:AppThemeBinding Dark={x:StaticResource Black}, Light={x:StaticResource White}}" />
            </pop:SfPopup.PopupStyle>

            <pop:SfPopup.ContentTemplate>
                <DataTemplate>
                    <Grid Padding="10" RowDefinitions="80,80,80,80,*">

                        <inputLayout:SfTextInputLayout Hint="{loc:Translate Ui_Name}">
                            <Entry />
                        </inputLayout:SfTextInputLayout>

                        <inputLayout:SfTextInputLayout Grid.Row="1" Hint="{loc:Translate Ui_Username}">
                            <Entry />
                        </inputLayout:SfTextInputLayout>

                        <inputLayout:SfTextInputLayout
                            Grid.Row="2"
                            EnablePasswordVisibilityToggle="True"
                            Hint="{loc:Translate Ui_Password}">
                            <Entry IsPassword="True" />
                        </inputLayout:SfTextInputLayout>

                        <inputLayout:SfTextInputLayout
                            Grid.Row="3"
                            EnablePasswordVisibilityToggle="True"
                            Hint="{loc:Translate Ui_Password}">
                            <Entry IsPassword="True" />
                        </inputLayout:SfTextInputLayout>

                        <HorizontalStackLayout
                            Grid.Row="4"
                            HorizontalOptions="End"
                            Spacing="10">
                            <app:CustomButton Command="{x:Binding ResetTexBoxCommand}" Text="{loc:Translate Ui_Clear}" />
                            <app:CustomButton Command="{x:Binding SaveToDatabaseCommand}" Text="{loc:Translate Ui_Save}" />
                        </HorizontalStackLayout>
                    </Grid>
                </DataTemplate>
            </pop:SfPopup.ContentTemplate>
        </pop:SfPopup>
    </Grid>

</ContentPage>
